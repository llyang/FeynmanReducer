This example demonstrate the ability to reduce linear combinations of Feynman integrals. Each linear combination should have a consistent mass dimension.

The configuration uses `targets_file: ["targets.txt", "targets.yaml"]` to demonstrate mixed target inputs. The text file contains single targets; the YAML file contains two linear combinations.

